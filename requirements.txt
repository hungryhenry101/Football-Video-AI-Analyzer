# Python dependencies only, for environments not created from environment.yml.
#
# The C++ dependencies (OpenCV, Ceres Solver, RapidJSON, Boost) are system packages,
# not Python ones — see core/BroadTrack/README.md

numpy
scipy>=1.16
matplotlib
# Base OpenCV only. Do not also install opencv-contrib-python: both provide the
# `cv2` module and installing them together overwrites files from the other,
# producing a broken mix. Nothing here uses a contrib API.
opencv-python
torch
tqdm
ultralytics
filterpy
pybind11
