# Python dependencies only, for environments not created from environment.yml.
# Note the scipy floor: see environment.yml for why scipy>=1.16 is required and
# why that in turn means Python >= 3.11.
#
# For the conda environment this project is developed against:
#     conda env create -f environment.yml && conda activate football
#
# The C++ dependencies for scripts/build_native.py (OpenCV, Ceres Solver,
# RapidJSON, Boost) are system packages, not Python ones — see
# core/BroadTrack/README.md.

numpy
scipy>=1.16
matplotlib
# Base OpenCV only. Do not also install opencv-contrib-python: both provide the
# `cv2` module and installing them together overwrites files from the other,
# producing a broken mix. Nothing here uses a contrib API.
opencv-python
torch
tqdm
ultralytics
filterpy
pybind11
